# Shared Layer

여러 화면에서 재사용하는 API 클라이언트, 포맷터, 공통 UI를 둡니다.

## 예시

- `api/`: 백엔드 fetch 래퍼
- `format/`: 원화/달러/퍼센트/날짜 포맷
- `ui/`: 버튼, 입력창, 결과 패널, 로딩 스피너
- `state/`: 가벼운 공통 상태 유틸

## 원칙

기능별 판단 로직은 `features/`에 두고, 이 폴더는 재사용 가능한 작은 도구만 둡니다.
