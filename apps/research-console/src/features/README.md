# Feature Layer

화면 단위 기능을 둡니다.

## 우선순위

1. `portfolio`: 현재 가장 상태가 복잡한 포트폴리오 편집/계산 화면
2. `dashboard`: 티커 상태 동기화와 다음 액션 카드
3. `capture`: 정보입력, 파일 업로드, 자동 분류 저장
4. `market-journal`: 시장일지 입력과 누적 활용
5. `memory`: 저장 데이터 조회, RAG 검색

각 기능은 화면 컴포넌트, 입력 검증, 해당 API 호출 훅만 가집니다. 분석 계산은 백엔드에서 처리합니다.
